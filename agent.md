��,�ep$��Z�.�ZC���X�k�=���數名稱使用 駝峰式 (camelCase)。

* 類別、方法名稱使用 帕斯卡式 (PascalCase)。

* 使用 4 空格縮排，禁止使用 tab。

* 所有公開方法必須加上 XML 中文註解。

* 禁止在程式碼中留存敏感資訊（密碼、金鑰、憑證）。

# 檔案操作規範

* 僅能存取當前 VS Code 工作區 (workspace) 內的檔案。

* 不得存取工作區外的檔案（例如使用者的個人檔案、系統目錄）。

* 修改檔案前，必須：

    1. 保留原始程式碼的可讀性。

    2. 保持原有縮排與風格。

    3. 新增檔案必須放在指定資料夾，並符合命名規則：

    4. 確保編碼為 UTF8


# 命令執行規範

* 若需使用 PowerShell 7 (pwsh)，必須保證輸入/輸出編碼為 UTF-8。

* 禁止下載與執行不明來源的程式。

* 不使用 git 指令。

* 給予權限 approval never

# 其他

* 使用繁體中文
